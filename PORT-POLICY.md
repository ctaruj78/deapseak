# 🔒 ПОЛІТИКА ПОРТІВ - DeapSeaK v2

## ⚠️ КРИТИЧНО ВАЖЛИВО

**ФІКСОВАНИЙ ПОРТ: 5000**

Вся система працює через **UNIFIED SERVER** на порту **5000**:
- ✅ Frontend (HTML/CSS/JS)
- ✅ REST API (`/api/*`)
- ✅ WebSocket (якщо використовується)
- ✅ Статичні файли

## 🚫 ЗАБОРОНЕНО

**НІКОЛИ НЕ ЗМІНЮЙТЕ ПОРТ БЕЗ ЯВНОГО ЗАПИТУ КОРИСТУВАЧА!**

### Файли з фіксацією порту:

1. **unified-server.js** (рядок 20-24)
   ```javascript
   const PORT = parseInt(process.env.DEAPSEAK_PORT || '5000', 10);
   ```

2. **assets/js/auth.js** (рядок 95-101)
   ```javascript
   static getApiUrl(endpoint) {
       return `${window.location.origin}${endpoint}`;
   }
   ```

3. **start-servers.sh**
   ```bash
   export DEAPSEAK_PORT=5000
   ```

## ✅ Переваги одного порту

- Немає CORS проблем
- Простіше для розробки
- Менше конфігурації
- Працює в Codespaces без додаткових налаштувань

## 📝 Історія

**06.12.2024** - Зафіксовано порт 5000 як єдиний порт для всієї системи після множинних проблем з багатопортовою конфігурацією.

---

**Якщо потрібно змінити порт - СПОЧАТКУ ЗАПИТАЙТЕ КОРИСТУВАЧА!**
