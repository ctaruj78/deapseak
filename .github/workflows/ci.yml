name: CI/CD Pipeline

on:
  push:
    branches: 
      - main
      - html-consolidation-phase1
  pull_request:
    branches: 
      - main

jobs:
  test:
    name: �� Run Tests
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x]
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      - name: 🧪 Run tests
        run: npm test
      
      - name: 📊 Generate coverage report
        run: npm test -- --coverage
      
      - name: 📈 Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
  
  lint:
    name: 🔍 Code Quality
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'
      - run: npm ci
      - name: Run linter
        run: npm run lint || echo "⚠️  Linter not configured, skipping..."
      - name: Check formatting
        run: npm run format:check || echo "⚠️  Prettier not configured, skipping..."
  
  build:
    name: 🏗️  Build Project
    runs-on: ubuntu-latest
    needs: [test, lint]
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'
      - run: npm ci
      - name: Build
        run: npm run build || echo "✅ No build step required"
  
  security:
    name: 🔒 Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18.x'
      - name: Run security audit
        run: npm audit --audit-level=moderate || true

