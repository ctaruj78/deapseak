# 📋 QR Code - Формат даних з адресою

**Дата:** 12 жовтня 2025  
**Статус:** ✅ РЕАЛІЗОВАНО

---

## 🎯 Проблема

Користувач вказав що **диспетчеру потрібно знати адресу** щоб підтвердити що технік відсканував QR код на правильному об'єкті.

---

## ✅ Рішення

### 1. Оновлений формат QR коду

**БУЛО (тільки номер):**
```
CML-123/456
```
❌ Диспетчер не знає де технік

**СТАЛО (компактний JSON):**
```json
{
  "id": "CML-123/456",
  "lift": 1,
  "addr": "вул. Тараса Шевченка, будинок 10"
}
```
✅ Диспетчер бачить локацію техніка

---

## 📊 Обмеження розміру

QR код (висока якість) підтримує **до ~2024 символів**.

### Приклади розміру даних:

| Формат | Приклад | Розмір | Статус |
|--------|---------|--------|--------|
| Тільки номер | `"CML-123/456"` | 11 символів | ✅ OK |
| Компактний JSON | `{"id":"CML-123/456","lift":1,"addr":"вул. Шевченка, 10"}` | ~70 символів | ✅ OK |
| Повний JSON зі старим форматом | `{"municipalNumber":"...","address":"...","accessUrl":"https://..."}` | **3452 символи** | ❌ OVERFLOW |

---

## 🔧 Технічна реалізація

### Файл: `enhanced-lift-modal.js`

```javascript
// Отримуємо дані
const address = $('#enhancedLiftAddress').val() || '';

// Створюємо компактний JSON
const qrData = {
    id: municipalNumber.trim(),
    lift: liftNumber,
    addr: address.substring(0, 80)  // Обрізаємо довгі адреси
};

const qrText = JSON.stringify(qrData);

// Генеруємо QR
new QRCode(container, {
    text: qrText,
    width: 150,
    height: 150
});
```

### Оптимізації:

1. **Короткі ключі**: `id`, `lift`, `addr` (замість `municipalNumber`, `liftNumber`, `address`)
2. **Обрізання адреси**: До 80 символів (достатньо для ідентифікації)
3. **Без timestamp**: Не потрібен для сканування
4. **Без accessUrl**: Занадто довгий (особливо в Codespaces)

---

## 📱 Використання

### Сценарій:

1. **Адміністратор** створює ліфт → генерує QR код
2. **QR код друкується** і клеїться на ліфт
3. **Технік приїжджає** на виклик
4. **Технік сканує QR** на місці
5. **Диспетчер бачить**:
   - ✅ Муніципальний номер ліфта
   - ✅ Номер ліфта в будинку
   - ✅ **Адресу об'єкта**
6. **Диспетчер підтверджує** що технік на правильній локації

---

## 🧪 Тестові файли

### 1. `qr-size-limits-test.html`
Показує різницю в розмірі даних:
- ✅ Тест 1: Тільки номер (11 символів)
- ✅ Тест 2: Компактний JSON (70 символів)  ← **НОВИЙ ФОРМАТ**
- ❌ Тест 3: Повний JSON (3452 символи) - показує стару проблему

### 2. `qr-scanner-demo.html` ← **НОВИЙ ФАЙЛ**
Емулює сканування QR техніком:
- Показує як диспетчер бачить дані
- Виділяє адресу в окремий блок
- Порівнює старий/новий формат
- Тестові кнопки для швидкої перевірки

**Відкрити:**
```
http://localhost:8080/qr-scanner-demo.html
```

---

## 📈 Результати

### До:
```
QR: "CML-123/456"
```
❌ Тільки номер  
❌ Адреса відсутня  
❌ Диспетчер не знає де технік  

### Після:
```json
{
  "id": "CML-123/456",
  "lift": 1,
  "addr": "вул. Шевченка, 10"
}
```
✅ Компактний JSON  
✅ Адреса є (до 80 символів)  
✅ Диспетчер бачить локацію  
✅ Не перевищує ліміт 2024 символи  

---

## 🔍 Перевірка

### Тест 1: Розмір даних
```bash
# Відкрити:
http://localhost:8080/qr-size-limits-test.html

# Результат:
# Тест 2 (компактний JSON) = ~70 символів ✅ OK
```

### Тест 2: Сканування
```bash
# Відкрити:
http://localhost:8080/qr-scanner-demo.html

# 1. Натиснути "✅ Тест: Правильний QR"
# 2. Побачити:
#    - Муніципальний номер
#    - Номер ліфта
#    - Адресу (виділено жовтим)
```

### Тест 3: Реальна генерація
```bash
# 1. Відкрити lifts.html
# 2. Додати новий ліфт
# 3. Заповнити:
#    - Муніципальний номер: CML-TEST-001
#    - Адреса: вул. Хрещатик, 1
# 4. Натиснути кнопку QR
# 5. Побачити QR код
# 6. Скопіювати дані (Console → "📦 QR text")
# 7. Вставити в qr-scanner-demo.html
# 8. Підтвердити що адреса відображається
```

---

## ✅ Висновок

1. **Диспетчер тепер бачить адресу** коли технік сканує QR
2. **Дані компактні** (70 символів замість 3452)
3. **Не перевищує ліміт** QR бібліотеки
4. **Зберігає важливу інформацію**: номер + ліфт + адреса
5. **Легко розширюється**: можна додати телефон, тип ліфта і т.д.

---

**Автор:** GitHub Copilot  
**Файли змінено:**
- `/assets/js/enhanced-lift-modal.js` (формат QR даних)
- `/qr-size-limits-test.html` (тести розміру)
- `/qr-scanner-demo.html` (емулятор сканування) ← **НОВИЙ**
